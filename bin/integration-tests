
#! /usr/bin/env bash
# docker compose up --exit-code-from integration-tester

# WORDPRESS_VERSION=$@

export WORDPRESS_VERSION="$1"
echo "$WORDPRESS_VERSION"

docker compose --project-directory "$PWD" up --exit-code-from integration-tester
# docker compose up --exit-code-from integration-tester
# docker compose config